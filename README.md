# GGST RCC Trainer

A minimalist, elegant web app for practicing Guilty Gear Strive Fast Roman Cancel (RCC) timing.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
